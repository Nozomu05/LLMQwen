# Python
.venv/
venv/
QWEN/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Environment variables
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific

storage/.ingest_cache.json
ingestion_errors.log
tempdocs/

# SearXNG (bare-metal web search backend, set up by searxng/setup.sh) —
# vendored upstream source and its venv are not tracked, only our own config
searxng/src/
searxng/.venv/

# Office temporary files
~$*
# Ignore everything inside docs/ but track each agent subfolder via .gitkeep
docs/*
!docs/*/
docs/*/*
!docs/.gitkeep
!docs/*/.gitkeep
# Ignore everything inside storage/ but not the folder itself
storage/*
!storage/
*.tmp



#Ignore vscode AI rules
.github\instructions\codacy.instructions.md
